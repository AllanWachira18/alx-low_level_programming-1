#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    putchar('0' + 0);  /* Print '0' character using its ASCII value */
    putchar('\n');

    return (0);
} 
